<%
  # Sub navigation footer partial
  # Expected argument:
  #  - navitems: array of navigation items containing img, url and title.
%>

<ul>
  <% @navitems.each do |nav| %>
    <li><a href="<%= nav[:url] %>"><%= nav[:title] %></a></li>
  <% end %>
</ul>

