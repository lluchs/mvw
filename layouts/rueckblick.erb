<h1><%= item[:headline] %></h1>

<% unless (media = get_media_items_for(item)).empty? %>
  <% unless item[:no_content] %>
    <nav class="pure-menu pure-menu-horizontal">
      <ul class="pure-menu-list">
        <li class="pure-menu-item"><a class="pure-menu-link" href="#rueckblick">Rückblick</a></li>
        <li class="pure-menu-item"><a class="pure-menu-link" href="#presse">Presse</a></li>
      </ul>
    </nav>
  <% end %>

  <section id="presse" class="<%= item[:no_content] ? 'visible' : '' %>">
    <h2>Presse</h2>
    <% media.each do |m| %>
      <img src="<%= m.path %>" alt="">
    <% end %>
    <div class="caption"><%= item[:media] %></div>
  </section>
<% end %>

<section id="rueckblick">
  <%= yield %>
</section>
